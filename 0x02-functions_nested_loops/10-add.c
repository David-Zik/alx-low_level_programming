#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: variable that holds an integer.
 * @b: varaible that holds an integer.
 * Return: the added integer
 */

int add(int a, int b)
{
		return (a + b);
}
